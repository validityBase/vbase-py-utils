# Generated from validityBase/github-configuration templates.
# Do not edit rendered files manually; update the source template in validityBase/github-configuration.
version: 2
updates:
  - package-ecosystem: "pip"
    # Scan /requirements as the single pip-compile graph. Its dev.in follows
    # ../requirements.in, so Dependabot includes the root runtime input without
    # processing the same graph as a second directory.
    directory: "/requirements"
    schedule:
      interval: "daily"
      time: "00:00"
      timezone: "Etc/UTC"
    versioning-strategy: "increase-if-necessary"
    groups:
      pip-version-updates:
        applies-to: "version-updates"
        patterns:
          - "*"
      pip-security-updates:
        applies-to: "security-updates"
        patterns:
          - "*"
    ignore:
      - dependency-name: "pip"
        update-types:
          - "version-update:semver-major"
          - "version-update:semver-minor"
